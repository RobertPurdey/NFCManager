package robert.purdey.nfcmanager.nfchandling;

/**
 * Purpose:
 *      Handles writing a string of data to an NFC tag.
 *
 * Developers:
 *  Danny Lieu & Robert Purdey
 *
 * Date:
 *  Nov 11 2017
 *
 * Revisions:
 */
public class NFCWriter
{
    // write to nfc
    public boolean writeTag(String payload)
    {
        boolean isValid = false;

        return isValid;
    }

    // validate nfc tag can be written to
    private boolean validateCanWrite(String payload)
    {
        boolean isValid = false;

        return isValid;
    }
}
